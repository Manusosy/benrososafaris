import { createLegalPageMetadata, createLegalPageRoute } from '@/lib/public/legal-page-route';

export const generateMetadata = createLegalPageMetadata('cookie-policy');
export default createLegalPageRoute('cookie-policy');
